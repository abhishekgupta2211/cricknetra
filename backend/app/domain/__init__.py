"""CricNetra domain core — framework-independent cricket logic.

This package is pure Python (Pydantic only) with no FastAPI / DB imports, so the
scoring + rules engine can be unit-tested in isolation and reused anywhere.
"""
