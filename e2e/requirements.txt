# End-to-end (browser) test deps. Install into the backend venv, then fetch the
# browser binary:
#   backend/.venv/Scripts/python -m pip install -r e2e/requirements.txt
#   backend/.venv/Scripts/python -m playwright install chromium
pytest-playwright>=0.7,<1.0
playwright>=1.50
